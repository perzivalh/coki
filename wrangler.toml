name = "coki"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".open-next/cloudflare"

[vars]
# Runtime env vars are set in Cloudflare Pages dashboard → Settings → Environment Variables
# NEXT_PUBLIC_* must ALSO be set as Build variables (so they embed during the build step)
